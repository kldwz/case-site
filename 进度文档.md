# 案例库网站 · 进度与计划文档

> 文档用途：记录项目当前进度、后续待办、关键文件位置、网站访问地址。
> 其他 Agent 接手「每日自动流水线」时，先读这份文档。
>
> **工作约定**：每完成一个里程碑，主 Agent 必须：(1) 更新任务清单状态；(2) 在下方「项目日志」按日期追加一条记录（做了什么、产出在哪、验证结果）；(3) 同步本页相关小节。日志可视为本项目的「带日期 changelog」。

---

## 一、项目目标

做一个**国内外创业 / 独立开发者收入案例库**网站（对标 Starter Story 中文版）：

- 每个案例讲清：**它怎么赚钱、流量从哪来、普通人能学什么**
- 风格：语言简单明了，不堆砌，讲透模式
- 内容：国内外都要，含 **Chrome 插件** 类；数字缺失就填「未知」，绝不虚构
- 最终形态：**每天自动**从固定源拉案例 → 生成中文案例页 → 构建 → 推 GitHub → 部署，用户无需重复操作

---

## 二、当前进度（已完成的）

### ✅ 网站本体（已跑通，本地可访问）
- 技术栈：Astro 静态站（`case-site/`）
- 结构：首页（案例列表 + 标签筛选）+ 详情页（8 字段数据卡 + 叙事正文 + 配图）
- 已收录 **8 个真实案例**，全部带真实截图、数据有公开来源、无虚构：

| # | 案例 | 模式 | 来源 |
|---|------|------|------|
| 1 | Calculator.net | 程序化 SEO + 广告 | 国外 |
| 2 | Levels.fyi | UGC 薪酬库 + 双边收费 | 国外 |
| 3 | Myinstants | UGC 音效站 + 广告 | 国外 |
| 4 | Refactoring UI | 电子书一次买断 | 国外 |
| 5 | RyRob | 联盟 + 课程 | 国外 |
| 6 | ShipFast | 模板一次买断 | 国外 |
| 7 | Dark Reader | Chrome 插件 + 开源捐赠 | 国外 |
| 8 | flomo 浮墨笔记 | 国内独立开发 + 订阅 SaaS | 国内 |

- 部署配置已就绪：`.github/workflows/deploy.yml`（推 GitHub 后自动部署 Pages）

### ✅ 信息源配置（你最初提的站全在里面）
- 文件：`case-site/pipeline/config/sources.yaml`
- 共 **13 个源**，分三类：

| 状态 | 国外 | 国内 |
|------|------|------|
| ✅ 已通，每天自动拉 | **trustmrr**（Supabase API，直接拿结构化 MRR）、IndieHackers、HN Show、ProductHunt | v2ex（只收分享创造/创业节点）、小众软件、少数派、36氪（均关键词过滤） |
| ⏸ 受限（已加配置，待处理） | StarterStory、Failory、MoneyMouth（反爬拦截）、**Chrome 商店**（Shadow DOM + 地区重定向抓不到，插件类案例靠 HN/IH 的 extension 关键词 + 人工补） | 即刻（feed 空壳）、独立开发者日报（站失效）、公众号（后续单独做） |

> trustmrr 走的是站点下发给浏览器的公开 Supabase anon key（`sources.yaml` 里），一条 HTTP 请求拿到全表
> 含 monthly_revenue / total_revenue / verified / country，无需浏览器渲染。只收最近 30 天更新的记录，
> 避免首次运行把 ~230 条存量全灌进 inbox。

### ✅ 抓取脚本（已跑通）
- 文件：`case-site/pipeline/fetch.py`
- 功能：从各源拉候选 → 过滤（导航/用户主页/评论计数链接）→ 去重（对比已有案例）→ 输出 `inbox/当天.json`
- 2026-09-05 实测一轮 **54 条候选**：trustmrr 6（含 MRR 数据）、HN Show 7、ProductHunt 8、IndieHackers 24、国内 9
- 支持抓取方式：RSS / HTML / Supabase API / 浏览器渲染（JS 站）
- 源级配置：`link_allow`（链接白名单正则，防 JS 站抓导航）、`title_allow`（标题必须含其一，如 v2ex 只收 [分享创造]/[创业]）、`accept_all`（案例源跳过关键词）
- 网络调用自动重试 2 次；`--fresh` 清空去重记录重拉（排查用）
- JS 渲染等待策略为 `domcontentloaded` + 固定等待（`networkidle` 在广告/长连接站点会超时）

### 📋 项目日志（按日期倒序，最新在上）

#### 2026-09-05 · 里程碑：每日自动流水线全闭环跑通（fetch → LLM 写文章 → 截图 → build → push → 部署）

**完成**：
- `pipeline/write_cases.py` 换用 **Anthropic SDK 直连**（token hub `api.tokenshub.org`，模型 `deepseek-v4-flash`，~7s/篇，比 `claude -p --bare` 冷启动稳定）
- **重试兜底（用户拍板）**：失败重试最多 5 次，第二次起超时 +20s；5 次全败写 `pipeline/failures/<日期>.md` 供用户人工补写
- `write_cases.py` 内嵌 playwright 截图（venv 装 playwright + Chrome），失败生成占位 SVG 不造假图
- 修复：LLM 输出非 JSON / 思考草稿 / 截断的解析；`可迁移点` null 导致 build 失败；中文标题 slug 滤光；TSV 去重
- `run_daily.sh` 串联全链路（fetch → write_cases → build → commit → push），push 走 `env -u` 清除代理（本机代理对 git CONNECT 隧道 503）
- launchd 注册 `com.casesite.daily` 每天 8:30 自动跑；日志写 `logs/daily_<日期>.log`
- **首次全量验证**：11 条候选全部处理完，8 篇新生成，4 篇靠重试兜底成功；截图验证通过
- 线上部署：首页 + 全部新增案例 HTTP 200

**当前规模**：25 条案例（8 手工 + 17 自动生成），11 个信息源，每日 8:30 自动跑。

**已知待办**：
- `app.md`（StoreShot）/ `v.md`（WhatToBuild）slug 过短——中文标题滤光导致，后续 fetch 自然纠正，非阻塞
- 独立 `screenshot.py`（流量数据图）未拆——SEMrush/SimilarWeb 反爬
- 公众号源接入（后续）

#### 2026-09-05 · 里程碑：GitHub Pages 部署闭环跑通
**完成**：
- `case-site/` 下 `git init`，初始化 commit（含 8 案例 + 图片 + 进度文档）
- 推送 `https://github.com/kldwz/case-site`（用户名 kldwz），token 权限踩坑修复：
  - 原 fine-grained PAT 只有读权限 → 403，改走 `gh auth login -w` 浏览器 OAuth 授权
  - OAuth token 只有 `repo` scope → push workflow 文件被拒，`gh auth refresh -s workflow` 补 scope
  - keychain 里旧 token 覆盖新 token → 手动 erase + store
- 写 `.github/workflows/deploy.yml`（checkout → setup-node → npm ci → astro build → configure-pages → upload-pages-artifact → deploy-pages）
- `astro.config.mjs` 设 `base: '/case-site/'`，`site: 'https://kldwz.github.io'`
- 全部 `/cases/...` 路径改 `/case-site/cases/...`（8 个 md + Base.astro + index.astro + [slug].astro），适配项目 Pages 子路径
- 手动在仓库 Settings → Pages 开启 GitHub Actions 部署（API 开启网络 503 失败，改手动）
- 部署成功：`https://kldwz.github.io/case-site/` HTTP 200，详情页 + 图片均 200

**验证**：workflow run 全绿（Build → Setup Pages → Upload → Deploy），线上首页/详情页/图片三处 HTTP 200。

**未完成**：汇总表、写文章脚本、截图脚本、run_daily.sh、定时注册（见下方分步计划 1-4、6）

#### 2026-09-05 · 里程碑：候选质量修复（fetch.py 重构）
**问题**：① trustmrr 用 JS 渲染抓不到（networkidle 超时）；② IndieHackers/36氪 首页把导航、用户主页（ohad1976）、评论链接（"6 comments"）当候选；③ v2ex 灌水（机场/VPS/问与答）混入；④ 少数派/小众软件/36氪 把纯资讯当案例收；⑤ Chrome 商店分类页 0 命中。

**处理**（`pipeline/fetch.py` + `pipeline/config/sources.yaml`）：
- trustmrr 从 JS 渲染改为 **Supabase 公开 API**（新 method `supabase`）——一条 HTTP 请求拿全表结构化数据（MRR/累计/verified/国家），无需浏览器
- 源级新增 `link_allow`（链接白名单正则：IH 只收 /post|product/，36氪只收 /p/）、`title_allow`（v2ex 只收 [分享创造]/[创业]）
- 丢弃导航词 / 单段无连字符路径（用户名主页）/ 纯计数文本（"N comments"，修了 link_allow 提前 return 跳过的 bug）
- 国内媒体源（小众软件/少数派/36氪）`accept_all` 改关键词过滤，融资/资讯自动出局；NOISE 加 vps/机场/加速器
- 全网络调用自动重试 2 次；JS 等待改 `domcontentloaded`+固定等待；新增 `--fresh`
- Chrome 商店源停用（Shadow DOM + 地区重定向，插件类案例靠其他源 extension 关键词 + 人工补充）

**验证**：`--fresh` 全量跑通，54 条候选/轮（trustmrr 6 含 MRR、HN 7、PH 8、IH 24、国内 9），IndieHackers 零评论噪音，`_nav_like` 8 组单测全过。

**当前能做到 / 还没做到**：
- ✅ 8 个源每日拉候选并过滤去重（含 trustmrr 结构化 MRR 数据）
- ✅ 本地 `astro build` / `astro preview`，8 个案例可访问
- ⏳ 自动写中文案例、自动截图、每日定时流水线、git 部署、公众号源——均未闭环（见第三节）

---

## 三、后续待办（交给其他 Agent 跑「每日自动流水线」）

目前只做到「① 每天拉候选」。剩下 3 步未自动闭环：

### 🚧 本次任务（2026-09-05 启动，目标：跑通「每天一条完整案例」闭环）

**流水线流程**（用户拍板，2026-09-05）：
`定时任务 → fetch 拉候选 → 写本地案例文件（inbox + 汇总表 + md）→ 截图 → astro build → git push → GitHub Actions 部署 Pages`

**分步计划**：
1. ✅ **汇总表**：`cases.tsv` 已建，字段：标题 | 创建时间 | 来源 | 备注 | 推送网站 | 推送公众号 | 推送头条号（✅/❌）。`write_cases.py --append` 自动追加行
2. ✅ **文章生成脚本**：`pipeline/write_cases.py`——读 `inbox/当天.json`，生成 `src/content/cases/<slug>.md`（8 字段 + 正文，未知填「未知」不虚构）。**已换用 Anthropic SDK 直连**（token hub：`deepseek-v4-flash`，7s/篇，比 `claude -p --bare` 冷启动稳定）
3. ✅ **截图**：内嵌于 `write_cases.py`（playwright 截官网首页，失败占位 SVG，不造假图）。已在 venv 装 playwright，Chrome 截图实测通过
4. ✅ **每日编排**：`run_daily.sh` 串联：fetch → 汇总表 → 写文章 → 截图 → `astro build` → `git commit + push`（push 用 `env -u` 清代理，避免本机代理 503）
5. ✅ **git 初始化 + GitHub 仓库 + Pages 部署**：已推 `kldwz/case-site`，`deploy.yml` 自动部署，线上 `https://kldwz.github.io/case-site/` 已 200
6. ✅ **定时注册**：macOS launchd `com.casesite.daily` 每天 8:30 跑 `run_daily.sh`，已加载验证

**当前阻塞（已解决）**：
- ✅ 项目已 git init 并推送到 `kldwz/case-site`
- ✅ `.github/workflows/deploy.yml` 已建，Pages 部署线上跑通
- ✅ **LLM 生成方案定案**：Anthropic SDK 直连 token hub（`api.tokenshub.org` + `ANTHROPIC_AUTH_TOKEN`），模型 `deepseek-v4-flash`（token hub 仅支持此模型，`claude-*` 均 404）。已实测 6.9s/篇稳定
- ✅ **重试兜底（用户拍板，2026-09-05）**：每次失败重试最多 5 次，第二次起超时 +20s；5 次全败写 `pipeline/failures/<日期>.md`，用户人工补写，脚本不再重试
- ⏳ 图源：SEMrush/SimilarWeb 反爬，流量图先做占位；官网图优先真实截图

### ✅ 待办 1：自动写中文案例（已完成，2026-09-05）
- `write_cases.py` 读 `inbox/当天.json`，为候选生成 `src/content/cases/<slug>.md`（8 字段 + 叙事正文）
- LLM 生成方案：Anthropic SDK 直连 → 失败退回 `claude -p --bare`
- 要求：语言简单、讲透「怎么赚钱 + 能学什么」，数字有来源才写否则「未官方披露」
- **2026-09-05 全量验证**：11 条候选全部处理完，8 篇新生成（boykli / Agent Builder Pro / 岁时 Sundial / PushUp Club / Flouie / Google AI Pro / 万兴科技 / shipped），4 篇靠重试兜底成功

### ✅ 待办 2：自动截图（已完成，2026-09-05）
- 内嵌在 `write_cases.py`（playwright 截官网首页，失败生成占位 SVG，不造假图）
- venv 已装 playwright，Chrome 实测截图通过
- 独立 `pipeline/screenshot.py` 后续如需补流量数据图再拆

### ✅ 待办 3：定时 + 构建 + 推送 + 部署（已完成，2026-09-05）
- `case-site/run_daily.sh` 串联：fetch → 写文章 → 截图 → `astro build` → `git commit + push`
- macOS launchd `com.casesite.daily` 每天 8:30 跑，日志写 `logs/daily_<日期>.log`
- git push 触发 GitHub Actions 自动部署 Pages（线上已 200 验证）

### ⏸ 后续（用户点头后再做）
- 公众号源接入（依赖 wechat-feeds RSS 服务，已有 `wechat-monitor` 工具可复用）
- 国内案例合集补充
- SEO / sitemap / RSS 打磨

---

## 四、关键文件索引

| 文件 | 作用 |
|------|------|
| `case-site/进度文档.md` | 本文档 |
| `case-site/pipeline/config/sources.yaml` | 13 个信息源配置 |
| `case-site/pipeline/fetch.py` | 每日拉候选脚本（已跑通） |
| `case-site/pipeline/screenshot.py` | 截图脚本（待写） |
| `case-site/run_daily.sh` | 每日编排脚本（待写） |
| `case-site/src/content/cases/*.md` | 每个案例一个文件，8 字段 + 正文 |
| `case-site/src/content/config.ts` | 案例字段 schema（8 字段） |
| `case-site/.github/workflows/deploy.yml` | 推 GitHub 后自动部署 Pages |
| `case-site/README.md` | 网站使用文档（新增案例流程、本地开发、部署） |

---

## 五、网站访问地址

### 本地预览（当前已在运行）
- 首页：http://localhost:4399/
- 案例列表同上，详情页：`http://localhost:4399/cases/<slug>/`
  - 例：http://localhost:4399/cases/calculator-net/
  - 例：http://localhost:4399/cases/darkreader/
  - 例：http://localhost:4399/cases/flomo/

> 本地地址仅在你这台 Mac 上可开，其他人访问不了。

### 部署到公网（GitHub Pages，已配置好但未执行）
1. 在 GitHub 新建仓库（如 `case-lib`）
2. 本项目根目录 `git init` → `git add .` → `git commit` → `git push` 到 `main`
3. 仓库 **Settings → Pages → Source** 选 **GitHub Actions**
4. 之后每次 `git push` 自动构建 + 部署
5. 访问：`https://<你的用户名>.github.io/<仓库名>/`
6. 如需自定义域名或干净路径：编辑 `case-site/astro.config.mjs`，取消 `base` 注释并改成仓库名

---

## 六、交接给其他 Agent 的注意事项

1. **数字规矩**：案例里所有收入/流量数字，有公开来源就标来源，没有就写「未官方披露 / 未知」，**绝对不编造精确数字**。
2. **截图规矩**：只放真实截图，截不到的用占位 SVG，不伪造收入图。
3. **源的配置**：`sources.yaml` 里 `enabled: false` 的源是受限的（反爬/失效），不要强行拉，先放着。
4. **去重**：`fetch.py` 用 `seen.json` 去重，新跑前不要手动删它（除非要重拉）。
5. **构建命令**：`cd case-site && npx astro build`，产物在 `dist/`。
6. **本地预览命令**：`cd case-site && npx astro preview --port 4399`。
